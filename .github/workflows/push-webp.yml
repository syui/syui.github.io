on:
  push:
    paths:
      - 'static/img/*.png'

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: install squoosh
      env:
        WORKFLOW_FILE_PATH: ${{ github.workflow }}
        GITHUB_REPOSITORY: ${{ github.repository }}
        GITHUB_SHA: ${{ steps.vars.outputs.sha_short }}

      run: |
        npm i -g @squoosh/cli

    - name: convert webp
      run: |
        s=`git diff-tree --no-commit-id --name-only -r $GITHUB_SHA|grep "static/img/"|grep "\.png"`
        echo $s
        for i in $s 
        do
        squoosh-cli --webp '{"quality":100}' -d ./static/img/min --resize '{width:300,height:400}' $i
        done
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        if [ -z "`git status -s`" ];then
          exit
        fi
        git add ./static/img/min
        git commit -m "push webp"

    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: src
